mvn spring-boot:run

    # sudo systemctl start docker
    # sudo systemctl stop docker
    # sudo systemctl status docker

# --- COMMANDS CHEAT SHEET ---
# Start:              docker-compose up -d
# View Status:        docker-compose ps
# Stop & Remove:      docker-compose down
# Delete Volume:      docker-compose down -v
# Delete Images:      docker rmi postgres:16-alpine redis:7-alpine
# Full Cleanup:       docker-compose down -v && docker rmi postgres:16-alpine redis:7-alpine



Swagger UI: http://localhost:8080/bleep-learnhub-backend/api/v1/swagger-ui/index.html
OpenAPI Specs (JSON): http://localhost:8080/bleep-learnhub-backend/api/v1/v3/api-docs


https://docmedhos.develeganceinnovatech.in/adtApi/swagger-ui/index.html#/Admission/admitPatient

hydraze1@hydraze1-Nitro:~/Desktop/code/github/ADT-UI/src/hooks$ ls

AdtApi  ApiUrls.ts  CSNOserv  CredentialApi  DrugApi  Icd10Api  LabApi  MasterApi  OrderApi  PatientApi

hydraze1@hydraze1-Nitro:~/Desktop/code/github/ADT-UI/src/hooks$ cd AdtApi/

hydraze1@hydraze1-Nitro:~/Desktop/code/github/ADT-UI/src/hooks/AdtApi$ ls

adt.type.ts  hooks

hydraze1@hydraze1-Nitro:~/Desktop/code/github/ADT-UI/src/hooks/AdtApi$ cd hooks/

hydraze1@hydraze1-Nitro:~/Desktop/code/github/ADT-UI/src/hooks/AdtApi/hooks$ ls

Admission.hook.ts  Dashboard.hook.ts  DeathCase.hook.ts  Discharge.hook.ts  Examination.hook.ts  Transfer.hook.ts  Treatment.hook.ts

hydraze1@hydraze1-Nitro:~/Desktop/code/github/ADT-UI/src/hooks/AdtApi/hooks$ 










docker exec -it bleep-postgres psql -U myuser -d learnhub_db -c "ALTER TABLE batches DROP COLUMN scheduled_date; ALTER TABLE batches DROP COLUMN scheduled_time;"















so i need some apis that only partner can access 

get all courses , aget batch by course and get session by batch and course 

course data all thins and in batch data also must contan course id and course name and in session data must contain course id , course name and batch id and batch name 

and then 
my courses that is course by partner id that the partnder have access to 
my bathc that is bathc by partnder id and course id  that the partnder have access to 
and session by partnder id , batch id and course if that has partnder accsss to  

if the access status is pending , rejected or approved send alll the data if the is a access request for that 

if approve in the session api whwre data will be fethed by partner id and course id and batch id then send data properly 

if pending dont send the session data if pending 

if rejected then slao dont send the session data 

and in the session data also send the max limit and exuisting count of students and if exides red flag this data in session data fetched by partnder batch and course id 

this are partnder specific apis 

another apis shedule or calender api 

thet will return data the user will send from date and to date and in edsponse will return in what day what session is there and session data that must cntain al session data and course and batch name too with course and batch id  

create a seperate controller for this and create a plan with what each api url request body and response body and and validation 








now go through every apis controller congif and repository ,. dto , entity enum , exceptionand repository security and service and check if every thisng is ok any code that might caouse error or problem later every escept of this project evry part table api logic evry thing is handeled properly everything and fix issue if ther s any 